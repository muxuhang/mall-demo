import { StyleSheet } from "react-native";
import { color } from ".";

export default StyleSheet.create({
  ROOT: {
    backgroundColor: color.background
  },
  FlexRowBox:{
    display:'flex',
    flexDirection:'row',
    alignItems:'center'
  }
})